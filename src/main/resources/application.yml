spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sms_tool?serverTimeZone=GMT%2b8
    username: root
    password: root
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 2000

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/*.xml

files:
  upload:
    path: /Users/xiaodu/IdeaProjects/SmsTool/files/